extends layout

block content
  div#tile_user
    h2= title
    div.g-signin2(data-onsuccess='onSignIn')
    div#tip_register register or login using your Google account.
    div.data
      p Hello, 
        span.username_print
        img(onclick='signOut()' src='/images/exit.png' style='margin-left: 20px; opacity: .8')
    div#toolbar
      .tool_btn#newItem_btn + New Item
  #form1
    textarea
    //
        p profile details
        p#email
      
      
  div#document
    #menu
      #collections
        div.menu_btn(v-for='collection in collections')
          h2 {{collection.name}}
      #categories
        div.menu_btn(v-for='category in categories') {{category}}
    table#maintable